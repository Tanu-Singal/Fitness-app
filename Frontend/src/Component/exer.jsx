import React from 'react'
import Exercise from './Exercise'

const exer = ({ exer, setExer, bodyPart }) => {
  return <Exercise exer={exer} setExer={setExer} bodyPart={bodyPart} />
}

export default exer
